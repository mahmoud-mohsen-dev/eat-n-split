export enum InputType {
    text = "text",
    number = "number",
    select = "select",
}

export interface FriendType {
    id: number;
    name: string;
    image: string;
    balance: number;
}
